export default function ContagemConcluir(props) {
  return (
    <div>
      <div>{props.enderecos}</div>
      <div>{props.itens}</div>
      <div>{props.lotes}</div>
      <div>{props.quantidades}</div>
    </div>
  );
}
